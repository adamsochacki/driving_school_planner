<%= flash[:error] if flash[:error] %>
<h3>Nowa lekcja</h3>
<%= form_for(@lesson, url: instructorlesson_path) do |l| %>
  <%= l.label :student_id %>
    <%= l.collection_select :student_id, User.where(is_an_instructor: false ), :id, :full_name %></br>
  <%= l.label :lesson_time %>
  <%= l.datetime_field :lesson_time %>
  <%= l.label :confirmed %>
  <%= l.check_box :confirmed %>
  <%= l.submit "Wyślij" %>
<% end %>